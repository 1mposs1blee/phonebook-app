import { Wrapper, NotFoundTitle } from './NotFound.styled';

const NotFound = () => {
  return (
    <Wrapper>
      <NotFoundTitle>Page not found :(</NotFoundTitle>
    </Wrapper>
  );
};

export default NotFound;
